# Хендофф сессии — проект для Степана

> Назначение: восстановить контекст в новом чате «с нуля». Прочитай этот файл,
> затем `roadmap.md`, `competitor-review.md` и `norm-selection-and-scoring.md`.
> Последнее обновление: июль 2026 (конец итерации 2).

## Как продолжить в новом чате (скопировать это)

> «Проект для Степана, репозиторий `methodspoint-afk/clinician`, ветка
> `claude/gifted-thompson-dsmsv5`. Прочитай `clinician-os/handoff.md` и
> `clinician-os/roadmap.md`, восстанови контекст и продолжаем. Демо:
> https://methodspoint-afk.github.io/clinician/»

Далее — либо задача #12 (развилка локально/сервер), либо новая обратная связь
Степана по итерации 2 (качественные методики).

## Что за проект

«Реестр патопсихологических методик» — веб-приложение (React + TypeScript + SQLite
в браузере через sql.js, локально, без ФИО испытуемых). Помогает клиническому
психологу: провести методику → сравнить результат с нормой → получить отчёт.
Ядро и отличие от конкурентов — **прозрачный подбор нормы Gate → Score**: сначала
отсев по популяции (возраст/образование/язык), потом балл качества нормы 0–100.
Приложение — поддержка решения специалиста, НЕ диагност (ключевой инвариант).

Заказчик — Степан (клинический психолог, тестирует MVP и даёт обратную связь).
Пользователь-владелец репозитория: methodspoint@gmail.com.

## Технические факты

- Код в `app/`. Разработка и деплой — ветка `claude/gifted-thompson-dsmsv5`.
- Демо публикуется автоматически при пуше (workflow `.github/workflows/deploy-demo.yml`,
  action peaceiris/actions-gh-pages → ветка gh-pages): **https://methodspoint-afk.github.io/clinician/**
- Проверка: `cd app && npx vitest run` (47 тестов) и `node e2e/acceptance.mjs`
  (нужен `npm run build` + `npm run preview -- --port 4173 &` перед e2e). Chromium
  предустановлен в `/opt/pw-browsers/chromium`.
- Данные/методики досеваются в существующие локальные базы автоматически
  (миграции v1–v3 + `seedIfEmpty` для методик и норм). Ключевые файлы:
  `domain/types.ts`, `domain/seedMethods.ts`, `domain/seedNorms.ts`,
  `domain/normSelection/{gate,score,selection,deviation}.ts`, `db/{schema,repositories}.ts`,
  `ui/{Examination,NormsScreen,SubjectCard,SummaryReport,QualitativeProtocol}.tsx`.

## Что сделано (хронология)

1. **MVP**: методики → замеры → подбор нормы (Gate→Score) → отчёт; локально, без ФИО.
2. **Публичное демо**: single-file HTML на GitHub Pages, защита от недоступного localStorage.
3. **Итерация 1** (обратная связь Степана): веса скоринга (свежесть D 15→5,
   источник E 10→20); возрастной Gate с жёсткими границами 16/60 и допуском ±10
   лет внутри взрослого диапазона (флаг `age_stretch`); формулировки вердиктов
   «снижение/повышение показателя относительно нормы условно здоровых»; 3 новые
   количественные методики (воспроизведение чисел, сравнение понятий, пиктограммы-
   количественная); поля «препараты» и «комментарий» в карточке (миграция v2);
   подсветка обязательных полей красным + обязательный год источника; «ближайшие
   нормы» вместо пустоты; отчёт по блокам (внимание/память/мышление) + динамика
   повторных обследований + экспорт .doc + комментарий специалиста по методике.
4. **Стартовая база норм**: 7 норм из бэкапа Степана («Методичка Бехтерева», Шульте
   ЭР/ПУ/ВР, «10 слов» p5/отсроченное) — `domain/seedNorms.ts`, досев при запуске.
   Тестового испытуемого из бэкапа НЕ добавляли (по прямой просьбе Степана).
5. **Обзор конкурентов и регуляторики**: `clinician-os/competitor-review.md`.
6. **Итерация 2** (качественные методики, задача #11): протокол-таблица без
   числовых норм. Методики «Исключение лишнего (4-й лишний)» (рубрикатор
   квалификации обобщения) и «Пиктограммы — протокол опосредования»
   (слово→образ→воспроизведённое слово). Система не подсказывает квалификацию —
   только копит базу (ТЗ раздел 7). Модель: `MethodConfig.qualitative`, миграция
   v3 (`test_results.qualitative`), `TestResult.qualitativeRows`, компонент
   `ui/QualitativeProtocol.tsx`. Нормы для качественных методик недоступны.

Итого методик: 9 (7 количественных + 2 качественные). Тесты: 47 юнит + e2e — зелёные.

## Дальше (согласованный порядок)

**Развилка #12 РЕШЕНА (июль 2026, слова Степана):** «Общая анонимная база —
главная фишка проекта. В этом его смысл — сопоставлять нормы и организовывать
сбор данных». Одновременно: **качественные/проективные — пауза** («норм по ним
не вывести»), фокус на подсчитываемых методиках; вопросник по итерации 2 снят.

План серверной дорожки: `clinician-os/shared-base-architecture.md`
(этапы С0–С3; С0 — файловый обмен слепками/нормами, можно делать без бюджета;
С1 — REST API + Postgres, ждёт решений владельца из §7 плана).

**Контур A — мониторинг публикаций и поиск норм** — исходная причина проекта
(цитата Степана в `contour-a-publications.md`; «постоянно сидишь, ищешь нормы,
вводишь»). План: A0 (ресёрч по запросу силами ИИ → карточки-кандидаты →
валидация Степаном, журнал в `clinician-os/research/`) → A1 (регулярный
протокол) → A2 (импорт кандидатов в приложение, формат С0) → A3 (серверный
конвейер после С1). Инварианты: кандидаты только «предложение», в базу — после
ручной валидации; о новых исследованиях уведомляем, автоподмены норм нет.

Далее: Контур A0 + этап С0 → (решения владельца) → С1 → С2 (нормы по
диагнозам) → С3 (командный режим, платный). Параллельно/позже — международный
рынок.

## Открытые вопросы

- К владельцу (перед С1): бюджет ~700–1300 ₽/мес, российский VPS, домен,
  список администраторов норм, консультация юриста по 152-ФЗ.
- К Степану: связывание повторных обследований для катамнеза; детали
  монетизации (~300 ₽/мес, командный режим платный).

## Рабочие договорённости

- **Перед каждым перерывом обновлять хендофф** (`handoff.md` + `roadmap.md`) и
  коммитить — это стандарт работы по проекту, напоминать не нужно.
- Все изменения — только в ветку `claude/gifted-thompson-dsmsv5`, с пушем
  (демо публикуется само). PR не открывать без явной просьбы.
- Модель идентификатор в артефакты/коммиты не писать.
